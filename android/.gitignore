gradle-wrapper.jar
/.gradle
/captures/
/gradlew
/gradlew.bat
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
key.properties
**/*.keystore
**/*.jks
# Exception: a throwaway key for sideloaded QA APKs, deliberately committed so
# every CI build signs with the same SHA-1 and Google sign-in keeps working.
# Its password is in app/build.gradle.kts. Never use it for a Play Store release.
!app/aivy-qa.keystore
